FROM {{ "build" | image_tag }} as build

FROM {{ "buster" | image_tag }}

USER root
COPY --from=build --chown=root /go/github.com/cert-manager/cert-manager/_bin/server/controller /usr/bin/controller

USER {{ "nobody" | uid }}
ENTRYPOINT ["/usr/bin/controller"]
