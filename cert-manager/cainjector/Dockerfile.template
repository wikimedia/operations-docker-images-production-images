FROM docker-registry.wikimedia.org/cert-manager/build:latest as build

FROM {{ seed_image }}

USER root
COPY --from=build --chown=root /go/github.com/jetstack/cert-manager/cmd/cainjector/cainjector /usr/bin/cainjector

USER {{ "nobody" | uid }}
ENTRYPOINT ["/usr/bin/cainjector"]
